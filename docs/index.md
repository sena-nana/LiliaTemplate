# 桌面应用脚手架

本仓库是最小 Tauri + Vue 应用脚手架。公共 UI、配置、工具、构建流程和 Tauri 窗口状态插件来自 LiliaUI；模板显式拥有 Vue Root、Router、Shell 组合、设置、命令、Overlay 与开发诊断装配，业务代码从 `src/features`、`src/routes.ts` 和 `src/commands.ts` 开始扩展。

## 开始

```bash
yarn install
yarn dev
```

更多命令见[开发启动](./guide/development.md)。
