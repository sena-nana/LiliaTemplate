# 桌面应用脚手架

本仓库是最小 Tauri + Vue 应用脚手架。公共 UI、配置、工具和构建流程来自 `@lilia/*` 包，应用代码从 `src/features`、`src/routes.ts` 和 `src/commands.ts` 开始扩展。

## 开始

```bash
yarn install
yarn dev
```

更多命令见[开发启动](./guide/development.md)。
